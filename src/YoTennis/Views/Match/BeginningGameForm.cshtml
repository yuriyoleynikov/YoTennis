@model StartGameCommand

<form asp-action="StartGame/@ViewBag.MatchId">
    <div class="match-container game-container">
        <input type="submit" value="Begin Game" class="btn btn-default" />
    </div>
</form>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
